<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pet Mania</title>
    <link rel="stylesheet" href="/css/style2.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    {{!-- Bootstrap ICONS --}}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css">
</head>
<body>
    <!-- header section starts  -->

<header class="header">

    <a href="#" class="logo"> <i class="fas fa-paw"></i> Pet Mania</a>

    <nav class="navbar">
        
            {{#if session.userid}}
                <a href="/tasks">Cadastrar-Pet</a>
                <a href="/tasks/exibir">Tarefas</a>
                <a href="/tasks/add">Criar Tarefa</a>
                <a href="/logout">Sair</a>
            {{else}}
                <a href="#home">Home</a>
                <a href="#about">Sobre</a>
                <a href="#shop">Produtos</a>
                <a href="#services">Serviços</a>
                <a href="#plan">Planos</a>
                <a href="#contact">Contato</a>
                <a href="/register">Cadastre-se</a>
            {{/if}}
    </nav>

    <div class="icons">
        <div class="fas fa-bars" id="menu-btn"></div>
        <a href="#" class="fas fa-shopping-cart"></a>
        <div class="fas fa-user" id="login-btn"></div>
    </div>

    <form action="/login" class="login-form" method="POST">
        <h3>Acessar Perfil</h3>
        <input type="email" name="email" placeholder="Digite seu email..."  class="box">
        <input type="password" name="senha" placeholder="Digite sua senha..."  class="box">
        <input type="submit" value="Entrar" class="btn">
        <div class="links">
            <spam>Não tem uma conta?</spam> <a href="/register">Clique aqui!</a>
        </div>
    </form>
   
   

</header>
   
    
    

    {{!-- Conteudo da Página --}}
    <div class="container">
        {{!-- Mensagens do SISTEMA --}}
        {{#if messages.message}}
            <div class="message">
                {{messages.message}}
            </div>
        {{/if}}

        {{#if messages.success}}
            <div class="success">
                {{messages.success}}
            </div>
        {{/if}}

        {{{body}}}
    </div>
    


   {{!-- <footer id="footer">
       <div class="corpo-footer"><span>Pet Mania &copy; 2022</span> </div>
    </footer> --}}
</body>
</html>